import React from 'react';

// Placeholder component for scoring UI
export default function ScoringPanel({ match }) {
  return (
    <div style={{ padding: '24px', textAlign: 'center', color: '#e2e8f0' }}>
      <h2 style={{ fontSize: '1.5rem', fontWeight: 800, marginBottom: '0.5rem' }}>Scoring Panel</h2>
      <p style={{ margin: 0, fontSize: '1rem' }}>Scoring interface will be displayed here.</p>
    </div>
  );
}
