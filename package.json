{
    "name": "the-turf-monorepo",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": "22.x"
    },
    "scripts": {
        "postinstall": "npm install --prefix backend",
        "start": "npm start --prefix backend",
        "build": "CI=false npm install --prefix client --legacy-peer-deps && CI=false npm run build --prefix client",
        "install-backend": "npm install --prefix backend",
        "install-client": "npm install --prefix client --legacy-peer-deps",
        "install-all": "npm install --prefix backend && npm install --prefix client --legacy-peer-deps"
    }
}