{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "client/build",
    "framework": "create-react-app",
    "cleanUrls": true,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}